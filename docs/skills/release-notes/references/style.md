# Release-note style

- One sentence per change, present tense, user perspective:
  "Adds X", "Fixes Y when Z" — never "Refactored the FooService".
- Name the feature the user sees, not the class that implements it.
- Breaking changes state what breaks AND the migration in the same line.
- No commit hashes, PR numbers, or author names in the text.
